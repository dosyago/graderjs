{
  "name": "grader",
  "version": "0.0.1",
  "description": "Template for cross-platform Node, JavaScript, HTML & CSS apps without the bloat. 'Massless' apps.",
  "main": "build/grader.js",
  "bin": {
    "myapp": "build/grader.js"
  },
  "scripts": {
    "stats": "npm run clean || : && rm -rf node_modules || : && getstats && cat stats/* || :",
    "postinstall": "bash ./scripts/build_setup.sh",
    "start": "npm run bundle && node build/grader.js",
    "build": "npm run bundle && npm run pack-binary",
    "bundle": "cd src/ && echo $(pwd) && npx webpack && cd ../ && ./scripts/webzip.js && echo $(pwd) && npx webpack",
    "pack-binary": "npm run pack-binary-nix && npm run pack-binary-win && npm run pack-binary-mac && npm run pack-binary-32",
    "build-nix": "npm run bundle && npm run pack-binary-nix",
    "build-win": "npm run bundle && npm run pack-binary-win",
    "build-mac": "npm run bundle && npm run pack-binary-mac",
    "pack-binary-nix": "npx nexe -t linux-x64 -o bin/grader.nix -i build/grader.js -r \"./build/*\"",
    "pack-binary-win": "npx nexe -t windows -o bin/grader.exe -i build/grader.js -r \"./build/*\"",
    "pack-binary-mac": "npx nexe -t macos-x64 -o bin/grader.mac -i build/grader.js -r \"./build/*\"",
    "pack-binary-32": "npx nexe -t windows-x32 -o bin/grader.win32.exe -i build/grader.js -r \"./build/*\" && npx nexe -t linux-x32 -o bin/grader.nix32 -i build/grader.js -r \"./build/*\"",
    "clean": "rm -rf build/ bin/ src/build/ dev/ || : && mkdir -p build && mkdir -p bin && mkdir -p src/build",
    "pack": "upx bin/grader*",
    "test": "npm run test-app",
    "test-svc": "node -r esm src/service.js",
    "test-app": "npm run bundle && node build/grader.js",
    "lint": "eslint 'src/**/*.js'",
    "tsc": "tsc --types node --target ES2020 --checkJs --allowJs --outFile build/server-types.js  --module System src/launcher.js decs.d.ts",
    "tsc-app": "tsc --types node --target ES2020 --checkJs --allowJs --outFile build/server-types.js  --module System src/app.js decs.d.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/c9fe/grader.git"
  },
  "keywords": [
    "devtools",
    "node",
    "hybrid",
    "desktop",
    "template",
    "electron",
    "sciter",
    "gradebr",
    "cross-platform apps",
    "chromium",
    "chrome"
  ],
  "author": "@dosy",
  "license": "AGPL-3.0",
  "bugs": {
    "url": "https://github.com/c9fe/grader/issues"
  },
  "homepage": "https://github.com/c9fe/grader#readme",
  "dependencies": {
    "adm-zip": "^0.4.16",
    "chrome-launcher": "latest",
    "esm": "latest",
    "express": "latest",
    "http-terminator": "^2.0.3",
    "node-fetch": "latest",
    "tcp-port-used": "^1.0.1",
    "ws": "latest"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.4.33",
    "@types/express": "^4.17.8",
    "@types/form-data": "^2.5.0",
    "@types/http-terminator": "^2.0.1",
    "@types/node-fetch": "^2.5.7",
    "@types/websocket": "^1.0.1",
    "@types/ws": "^7.2.7",
    "eslint": "^7.11.0",
    "nexe": "latest",
    "node-dev": "latest",
    "typescript": "^4.0.3",
    "webpack": "latest",
    "webpack-cli": "^4.1.0"
  }
}
